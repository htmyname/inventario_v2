<!DOCTYPE html>
<!--
This is a starter template page. Use this page to start your new project from
scratch. This page gets rid of all links and provides the needed markup only.
-->
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="x-ua-compatible" content="ie=edge">

    <title>{% block title %}Inventario - Log in!{% endblock %}</title>

    {% block stylesheet %}{% endblock %}
    <!-- Font Awesome Icons -->
    <link rel="stylesheet" href="{{ asset('plugins/fontawesome-free/css/all.min.css') }}">
    <!-- Theme style -->
    <link rel="stylesheet" href="{{ asset('dist/css/adminlte.min.css') }}">
    <style>
		* {
			user-select: none;
		}
        body {
            background-image: url("{{ asset('dist/img/bg2.jpg') }}");
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
        }
    </style>
</head>
<body>
{% block body %}
    <div class="p-3">
        <div class="container-fluid mt-5 col-md-4 p-5 bg-white" style="border-radius: 5px;box-shadow: 0.5px 0.5px 3px;">
            <form method="post">
                {% if error %}
                    <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}

                {% if app.user %}
                    <div class="mb-3">
                        You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
                    </div>
                {% endif %}

                <h1 class="h3 mb-3 font-weight-normal">Inicia Sesión</h1>
                <label for="inputUsername">Usuario</label>
                <input type="text" value="{{ last_username }}" name="username" id="inputUsername" class="form-control mb-3"
                       required autocomplete="off" {% if not error %}autofocus{% endif %}>
                <label for="inputPassword">Contraseña</label>
                <input type="password" name="password" id="inputPassword" class="form-control"
                       required autocomplete="off" {% if error %}autofocus{% endif %}>
                <input type="hidden" name="_csrf_token"
                       value="{{ csrf_token('authenticate') }}"
                >

                {#
                Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                See https://symfony.com/doc/current/security/remember_me.html #}

                <div class="checkbox mb-3 mt-3">
                    <label> <input type="checkbox" name="_remember_me"> Recuérdeme </label>
                </div>


                <button class="btn btn-primary btn-block mt-3" type="submit">
                    Entrar
                </button>
            </form>
        </div>
    </div>
{% endblock %}
</body>
</html>